# Foundry
cache/
out/
broadcast/
**/broadcast/**/dry-run/

# Hardhat
artifacts/
typechain/
typechain-types/
node_modules/

# Env
.env
.env.local
.env.*.local
*.key
*.pem

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Build artifacts
dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage
coverage/
lcov.info

# Agent artefacts (research-time scratch)
.playwright-mcp/
.claude-research/

# Local UI test artifacts (Playwright screenshots from lineastr-ui-tester subagent)
ui-test/
.playwright-mcp/
.vercel

# off-chain bot strategy lives in the private repo patronny/lineadat-keeper (kept out of this repo so it can be open-sourced without leaking the bot)
automation/keeper/

# Internal ops notes + local scratch - kept OUT of the public repo
obsidian/
REHEARSAL-RUNBOOK.md
CLAUDE.md
.ui-test-shots/
# root-level scratch screenshots (UI/design captures); real assets live under frontend/public/
/*.png

# Brand design assets / candidates (logos, banners, preview HTML) - local only; the
# chosen marks are inlined in frontend code and Hub images are uploaded manually.
brand/
